<?xml version="1.0" encoding="UTF-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="ring" android:useLevel="false">
            <padding android:bottom="5dp" android:left="1dp" android:right="1dp" android:top="5dp" />

            <solid android:color="@android:color/white" />

            <stroke android:width="8dp" android:color="@color/transparent" />

            <size android:width="15dp" android:height="15dp" />

            <!-- 矩形的圆角半径 -->
            <corners android:radius="5dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle" android:useLevel="false">
            <padding android:bottom="5dp" android:left="1dp" android:right="1dp" android:top="5dp" />

            <solid android:color="@android:color/transparent" />

            <stroke android:width="8dp" android:color="@color/transparent" />

            <size android:width="15dp" android:height="15dp" />

            <!-- 矩形的圆角半径 -->
            <corners android:radius="5dp" />
        </shape>
    </item>
</selector>